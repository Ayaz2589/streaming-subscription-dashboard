import { Movie, User } from "../dummyDataUtils/types";

const getHighestWatchedMovies = (movies: Movie[], users: User[]) => {
//   console.log({
//     movies,
//     users,
//   });

  // create a cache of movies with their counts. { id, count }

//   const movieCache = movies.reduce((acc, movie) => {
//     console.log(movie);
//     return acc
//   }, {});

//   console.log(movieCache)
};

export default getHighestWatchedMovies;
